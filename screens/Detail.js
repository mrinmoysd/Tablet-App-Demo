import * as React from 'react';
import {FlatList, Image, Text, View} from 'react-native';
import { COLORS, SIZES, dummyData, images, FONTS } from "../constants";

const Detail = ({item}) => {
    return (
        <View>

        </View>
    )
}

export default Detail